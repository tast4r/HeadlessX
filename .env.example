# REQUIRED: Security token for API authentication
# Generate a secure token with: openssl rand -hex 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN=5f734b3861446e32efea91080f51ffcdbf0e53ed298a61ba02a0a82c5f26e4d1
# Server configuration
PORT=3000
NODE_ENV=production

# Domain configuration (replace with your actual domain)
DOMAIN=saify.me
SUBDOMAIN=headlessx
# Full domain will be: headlessx.yourdomain.com

# Optional: Custom browser settings
BROWSER_HEADLESS=true
BROWSER_TIMEOUT=60000

# Optional: Performance tuning
UV_THREADPOOL_SIZE=128
NODE_OPTIONS=--max-old-space-size=4096